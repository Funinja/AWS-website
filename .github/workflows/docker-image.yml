name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  container-test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run Jest Test
        run: (docker build -t funinja/aws-website -f Dockerfile.dev .)
